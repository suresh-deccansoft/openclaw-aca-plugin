/**
 * GENERATED FILE — do not hand-edit.
 *
 * Produced by `pnpm --filter @{{PROJECT_SLUG}}/core generate`
 * (openapi-typescript against backend/openapi.json). See
 * reference/architecture-decisions.md #6 — this is how frontend error/response
 * types stay impossible to drift from backend reality: they're generated,
 * not hand-maintained.
 *
 * Exempted from the 1000-line rule in .eslintrc.cjs, since it's regenerated
 * wholesale rather than split by hand.
 *
 * Placeholder until the backend is running and `generate` has been run once:
 */
export interface paths {
  [path: string]: unknown;
}
